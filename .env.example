# Optional runtime environment for Robot Web Launcher.
# Copy to .env only if you want to run manually; systemd uses its own Environment lines.

ROBOT_LAUNCHER_CONFIG=/data/sinuo_project/robot_web_launcher/config/modules.yaml
ROBOT_LAUNCHER_AUTOSTART=0

